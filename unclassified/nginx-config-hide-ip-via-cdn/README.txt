Copy all contents to directory `/etc/nginx/sites-available/` to hide IP address when using CDN.

Since file `default` might already exist, it is better to backup it first.

See here for more information: https://wsgzao.github.io/post/nginx-default-server/ .
